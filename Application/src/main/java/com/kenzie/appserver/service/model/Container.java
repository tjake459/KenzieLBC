package com.kenzie.appserver.service.model;

// I am leaving this in the repo for the moment in case we decide to use it anyway, but as of 09/22, it
// seems like it might make things more difficult than it's worth to switch location to an enum instead of a String
public enum Container {
    PANTRY, REFRIGERATOR, FREEZER, CABINET, CLOSET, CUPBOARD, SHELF
}
